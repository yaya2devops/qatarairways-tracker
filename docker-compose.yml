services:
  tracker:
    image: ghcr.io/${GITHUB_REPOSITORY:-msamoeed/qatarairways-tracker}:latest
    restart: unless-stopped
    ports:
      - "7432:3000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/tracker.db
    volumes:
      - tracker-data:/app/data

volumes:
  tracker-data:
